DISCORD_TOKEN=your_discord_bot_token
DATABASE_URL=postgresql://user:password@host:port/dbname

# Only this Discord user can enable the bot in servers via /allow-bot
OWNER_ID=1150660855025909800

# Optional: speeds up slash-command updates during development (guild-only sync)
DEV_GUILD_ID=

# Optional: website/dashboard URL used in help/error messages
DASHBOARD_URL=

# If true, channels will be deleted shortly after closing
DELETE_CLOSED_TICKETS=false
